# React Dynamic Island example

`App.tsx` demonstrates a product-owned integration: semantic routing actions,
artifact renderers, Turkish state labels, and the reusable Dynamic Island.

The example expects the application backend to expose the HTTP/SSE endpoints
documented in `docs/ui-agent.md`. It intentionally contains no API key; model
credentials stay on the server.
